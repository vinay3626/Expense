import { HideSaveDirective } from './hide-save.directive';

describe('HideSaveDirective', () => {
  it('should create an instance', () => {
    const directive = new HideSaveDirective();
    expect(directive).toBeTruthy();
  });
});
